<aside>
  <slot />
</aside>

<style lang="scss">
  aside {
    border: 1px solid var(--foreground);
    border-left: 12px solid var(--foreground);
    padding: 1rem;
    margin-bottom: 1rem;

    p {
      margin: 0;
    }
  }
</style>
